---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Pagină negăsită - LocalPro">
  <Navbar />
  
  <main class="error-page">
    <div class="container">
      <div class="error-content">
        <div class="error-code">404</div>
        <h1>Pagină negăsită</h1>
        <p class="text-muted">Ne pare rău, dar pagina pe care o cauți nu există sau a fost mutată.</p>
        
        <div class="error-actions">
          <a href="/" class="btn btn--primary btn--lg">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
              <polyline points="9 22 9 12 15 12 15 22"/>
            </svg>
            Acasă
          </a>
          <a href="/categorii" class="btn btn--ghost btn--lg">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
            </svg>
            Caută servicii
          </a>
        </div>
        
        <div class="error-suggestions">
          <h3>Poate te interesează:</h3>
          <div class="suggestion-links">
            <a href="/categorie/stomatologie">🦷 Stomatologie</a>
            <a href="/categorie/auto">🚗 Service Auto</a>
            <a href="/categorie/frizerie">💇 Frizerii</a>
            <a href="/categorie/instalatii">🔧 Instalații</a>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <Footer />
</Layout>

<style>
  .error-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }
  
  .error-content {
    text-align: center;
    max-width: 500px;
  }
  
  .error-code {
    font-family: var(--font-display);
    font-size: clamp(6rem, 20vw, 12rem);
    font-weight: 700;
    background: var(--gradient-hero);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    margin-bottom: 1rem;
  }
  
  .error-content h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
  }
  
  .error-content > p {
    margin-bottom: 2rem;
  }
  
  .error-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 3rem;
  }
  
  .error-suggestions {
    padding: 2rem;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
  }
  
  .error-suggestions h3 {
    font-size: 1rem;
    margin-bottom: 1rem;
    color: var(--text-muted);
  }
  
  .suggestion-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
  }
  
  .suggestion-links a {
    padding: 0.5rem 1rem;
    background: var(--bg-secondary);
    border-radius: var(--radius-md);
    color: var(--text-primary);
    transition: all 0.2s;
  }
  
  .suggestion-links a:hover {
    background: var(--accent-primary);
    color: var(--bg-primary);
  }
  
  @media (max-width: 480px) {
    .error-actions {
      flex-direction: column;
    }
  }
</style>
